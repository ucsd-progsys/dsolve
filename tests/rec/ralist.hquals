(* from the mlq *)

qualif A(v) : sz l { * *} v

(* not from the mlq *)

qualif AA(v) : sz v = ~A
qualif B(v) : sz v = sz ~A {< +, - >} [0, 1]

qualif ZEROC(v) : v { * * } 0
qualif N(v) : v { * * } ~A 
qualif ZERO(v) : sz v { * * } 0
